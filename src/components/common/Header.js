import React from 'react';
import { Link, IndexLink } from 'react-router';

const Header = () => {
  return(
    <nav>
      <h1>Imgur Photo Viewer - Scroll down to load more images</h1>
    </nav>
  );
};

export default Header;
